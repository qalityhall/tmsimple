name: CI

on:
    push:
        branches: ['main']
    pull_request:
        branches: ['main']

jobs:
    check:
        name: Linter & Prettier
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - run: npm ci
            - run: npm run eslint
            - run: npm run prettier
